# E2E Tests (Playwright)

Auto-generated Playwright tests from Jira QA tickets.

## Setup
```bash
cd e2e && npm install && npx playwright install chromium
```

## Run
```bash
# Requires backend on :3000 and frontend on :5173
cd e2e && npm test
```
